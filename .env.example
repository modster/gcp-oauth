# Google OAuth client credentials
# Create credentials at: https://console.cloud.google.com/apis/credentials
GOOGLE_CLIENT_ID=your-client-id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your-client-secret

# Session secret — use a long, random string in production
# Generate one with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
SESSION_SECRET=change-me-to-a-random-secret

# Optional: publicly reachable origin (used to build the OAuth redirect URI)
# In production set this to e.g. https://your-app.example.com
# BASE_URL=http://localhost:3000

# Optional: port the OAuth/Express server listens on (default: 3000)
# PORT=3000

# Optional: port the Observable Framework dev server listens on (default: 3001)
# Only used when NODE_ENV is not "production"
# FRAMEWORK_PORT=3001
